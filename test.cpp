#define __STDC_LIMIT_MACROS
#include <stdint.h>
#include <limits>
#include "agg.h"
#include <zlib.h>
#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <vector>
#include <deque>
#include <string>
#include "string.h"
#include "utils.h"
#include "depthMerger.h"



using namespace std;


int main(int argc,char **argv) {
  // int buf_size=100;
  // vector<depthInterval> test1;
  // test1.resize(100);
  // circularBuffer test_buf;
  // depthInterval tmp_line;tmp_line.start=1;tmp_line.stop=2;tmp_line.depth=0;tmp_line.gq=0;tmp_line.chrom=-1;
  // test_buf.resize(buf_size);
  // for(int j=0;j<buf_size;j++)
  //     test_buf.push_back(tmp_line);
  // cerr<<"made dp_buf1"<<endl;


  return(0);
}
